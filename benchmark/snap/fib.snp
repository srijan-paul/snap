const clock = import('clock')

func fib(n) {
  if n <= 1 return 1
  return fib(n - 1) + fib(n - 2)
}

func fib_test() {
  const start = clock.now()
  for i = 1, 100 
    for i = 1, 20 
      fib(i)
  return clock.now() - start 
}

printf("Fibonacci snap: (ms) ", fib_test())
